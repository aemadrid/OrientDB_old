require '_lib/page'
require '_lib/menu'

desc "Migrate all files from the original wiki into github pages"
task :migrate do
  Page.migrate
end

desc "Generate the topbar menu"
task :menu do
  Menu.generate
end